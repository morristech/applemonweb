bugs:

 - Filtering ClientAdmin list by all still excludes inactive clients.
 - ModelSelect2 widget search containing string is not implemented.
 - LaTeX is not escaped.

features:

 - Import GnuCash phone and fax numbers.
 - Autocomplete invoice line item contents.
